package com.freshkeeper.model

enum class EventType {
    USER_JOINED,
    USER_ADDED,
    USER_LEFT,
    PRODUCT_ADDED,
    EDIT,
    NAME,
    CONSUMED,
    THROWN_AWAY,
    QUANTITY,
    QUANTITY_INCREASED,
    QUANTITY_DECREASED,
    EXPIRY,
    STORAGE,
    CATEGORY,
    DEFAULT,
}
